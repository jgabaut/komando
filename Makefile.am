#Generated by amboso v2.1.0
AUTOMAKE_OPTIONS = foreign
CFLAGS = @CFLAGS@
SHELL := /bin/bash
.ONESHELL:
MACHINE := $$(uname -m)
PACK_NAME = $(TARGET)-$(VERSION)-$(OS)-$(MACHINE)
komando_SOURCES = src/main.c src/komando.c
LDADD = $(KOMANDO_LDFLAGS)
AM_LDFLAGS = -O2
AM_CFLAGS = $(KOMANDO_CFLAGS) -O2 -Werror -Wpedantic -Wall
if DEBUG_BUILD
AM_LDFLAGS += -ggdb -O0
AM_CFLAGS +=
else
AM_LDFLAGS += -s
endif
if KLS_BUILD
komando_SOURCES += koliseo/src/koliseo.c
AM_CFLAGS += -DKMD_HAS_KOLISEO
endif

%.o: %.c
	$(CCOMP) -c $(CFLAGS) $(AM_CFLAGS) $< -o $@

$(TARGET): $(komando_SOURCES:.c=.o)
	@echo -e "    CFLAGS: [ $(CFLAGS) ]"
	@echo -e "    AM_CFLAGS: [ $(AM_CFLAGS) ]"
	@echo -e "    LDADD: [ $(LDADD) ]"
	$(CCOMP) $(CFLAGS) $(AM_CFLAGS) $(komando_SOURCES:.c=.o) -o $@ $(LDADD) $(AM_LDFLAGS)

clean:
	@echo -en "Cleaning build artifacts:  "
	-rm $(TARGET)
	-rm src/*.o
	-rm static/*.o
	@echo -e "Done."

cleanob:
	@echo -en "Cleaning object build artifacts:  "
	-rm src/*.o
	-rm static/*.o
	@echo -e "Done."

anviltest:
	@echo -en "Running anvil tests."
	./anvil -tX
	@echo -e "Done."

all: $(TARGET)
rebuild: clean all
.DEFAULT_GOAL := all
